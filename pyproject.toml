[project]
name = "dynamic-evaluation"
version = "0.0.0"
description = "Dynamic Contrastive Evaluation with Humans"
readme = "README.md"
license = { text = "apache-2.0" }
requires-python = ">=3.12"
authors = [{ name = "Vilém Zouhar", email = "vilem.zouhar@gmail.com" }]
keywords = [
    "evaluation",
    "machine translation",
    "human evaluation",
    "annotation",
]
dependencies = [
]

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Repository = "https://github.com/zouharvi/dynamic-evaluation"
Issues = "https://github.com/zouharvi/dynamic-evaluation/issues"

[tool.setuptools]
package-dir = { "dynamic_evaluation" = "src" }
packages = ["dynamic_evaluation"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

# pip install -e . --config-settings editable_mode=strict