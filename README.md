# 🫅🏻 DUKE
Dynamic Uncertainty Kway Evaluation

Work-in-progress project

## Experiments

Generally, only scripts in `scripts/` should be launched directly.
The code in `src/` is implementation of various DUKE algorithms and utility functions.

```bash
python3 ./scripts/01-pilot_simulation.py
```